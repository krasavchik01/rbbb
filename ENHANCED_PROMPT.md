# 🚀 УЛУЧШЕННЫЙ ПРОМПТ ДЛЯ РАЗРАБОТКИ

## [ТВОЙ ЗАПРОС]

Реализовать расширенную функциональность управления проектами:

1. **Папка проекта для файлов:**
   - Создать систему хранения файлов проекта (не только договор, но и сканы, скриншоты, Word документы, PDF и т.д.)
   - Загружать файлы в Supabase Storage в отдельную папку для каждого проекта
   - Отображать список всех файлов проекта с возможностью просмотра, скачивания и удаления
   - Интегрировать в форму создания проекта и в страницу проекта

2. **Этапы проекта (с чекбоксом):**
   - Добавить чекбокс "Этапы проекта" в форму создания проекта (CreateProjectProcurement.tsx)
   - При включении чекбокса открывать динамические строки для добавления этапов
   - Каждый этап: название (например, "Аудит за 6 месяцев"), дата начала, дата окончания
   - Сохранять этапы в структуре проекта

3. **Дополнительные услуги (с чекбоксом):**
   - Добавить чекбокс "Дополнительные услуги" в форму создания проекта
   - При включении открывать список для выбора/добавления услуг (обучение, семинар, консультация и т.д.)
   - Каждая услуга: название, описание (опционально), стоимость (опционально)
   - Сохранять в структуре проекта

4. **Доп соглашения для действующих проектов:**
   - Добавить функционал для отдела закупок для добавления доп соглашений/изменений в действующие проекты
   - Создать интерфейс для добавления доп соглашения: номер, дата, описание изменений, файл (опционально)
   - Сохранять историю доп соглашений в проекте
   - Отображать список доп соглашений в карточке проекта

## [ИНСТРУКЦИИ ПО MCP СЕРВЕРАМ]

**ОБЯЗАТЕЛЬНО используй следующие MCP серверы в указанной последовательности:**

### 1. **Sequential Thinking** (mcp_sequential-thinking_sequentialthinking) - ПЕРВЫМ ДЕЛОМ

Используй для глубокого анализа задачи:

**Шаг 1: Анализ текущей архитектуры**
- Изучи текущую структуру проекта (ProjectV3, ContractInfo)
- Проанализируй CreateProjectProcurement.tsx - где и как добавить новые поля
- Изучи supabaseDataStore.ts - как работает сохранение проектов
- Определи структуру таблиц в Supabase для хранения файлов, этапов, услуг, доп соглашений

**Шаг 2: Планирование изменений БД**
- Нужна ли новая таблица для файлов проекта или хранить в JSONB?
- Как хранить этапы проекта: отдельная таблица или в JSONB проектов?
- Как хранить дополнительные услуги: в JSONB или отдельная таблица?
- Как хранить доп соглашения: отдельная таблица с историей?

**Шаг 3: Планирование UI компонентов**
- Компонент для загрузки множественных файлов
- Компонент для управления этапами (динамическое добавление/удаление)
- Компонент для выбора дополнительных услуг
- Компонент для добавления доп соглашений (для отдела закупок)
- Компонент для отображения списка файлов проекта

**Шаг 4: Планирование интеграции**
- Где показать папку файлов проекта (в карточке проекта, в отдельной вкладке?)
- Как интегрировать этапы в существующую форму создания
- Как интегрировать доп услуги в форму
- Где и как показать возможность добавления доп соглашений (только для отдела закупок, только для действующих проектов)

**Шаг 5: Edge cases и безопасность**
- Валидация типов файлов и размера
- Ограничения на количество файлов
- Права доступа: кто может загружать/удалять файлы
- Что делать если проект удален - удалять ли файлы из Storage?
- Как обрабатывать ошибки загрузки файлов

### 2. **Context7** (mcp_context7_resolve-library-id, mcp_context7_get-library-docs)

Используй для получения актуальной документации:

**Для работы с Supabase Storage:**
- Вызови `mcp_context7_resolve-library-id` с запросом "supabase storage" или "@supabase/storage-js"
- Получи документацию через `mcp_context7_get-library-docs` с фокусом на:
  - Загрузку файлов в buckets
  - Управление файлами (удаление, получение URL)
  - Организацию файлов по папкам
  - Обработку ошибок

**Для работы с загрузкой файлов в React:**
- Вызови `mcp_context7_resolve-library-id` для "react-dropzone" или "file-upload"
- Получи best practices для множественной загрузки файлов

### 3. **Sequential Thinking** (продолжение) - детальное планирование

**Шаг 6: Детальная структура данных**

**Файлы проекта:**
```typescript
interface ProjectFile {
  id: string;
  projectId: string;
  fileName: string;
  fileType: string;
  fileSize: number;
  storagePath: string; // путь в Supabase Storage
  uploadedBy: string;
  uploadedAt: string;
  category?: 'contract' | 'scan' | 'document' | 'screenshot' | 'other';
}
```

**Этапы проекта:**
```typescript
interface ProjectStage {
  id: string;
  name: string; // "Аудит за 6 месяцев"
  startDate: string;
  endDate: string;
  description?: string;
}
```

**Дополнительные услуги:**
```typescript
interface AdditionalService {
  id: string;
  name: string; // "Обучение", "Семинар", "Консультация"
  description?: string;
  cost?: number;
}
```

**Доп соглашения:**
```typescript
interface ProjectAmendment {
  id: string;
  projectId: string;
  number: string; // номер доп соглашения
  date: string;
  description: string;
  fileUrl?: string; // файл доп соглашения
  createdBy: string;
  createdAt: string;
}
```

**Шаг 7: План миграции БД**
- Создать таблицу `project_files` для файлов проекта
- Расширить таблицу `projects` добавив JSONB поля: `stages`, `additional_services`
- Создать таблицу `project_amendments` для доп соглашений
- Создать bucket в Supabase Storage для файлов проектов (если не существует)

**Шаг 8: План реализации компонентов**
- Создать `ProjectFileManager.tsx` - компонент для загрузки и управления файлами
- Модифицировать `CreateProjectProcurement.tsx` - добавить чекбоксы и поля для этапов и услуг
- Создать `ProjectStagesEditor.tsx` - компонент для редактирования этапов
- Создать `AdditionalServicesSelector.tsx` - компонент для выбора услуг
- Создать `ProjectAmendmentForm.tsx` - форма для добавления доп соглашений
- Модифицировать страницу проекта для отображения файлов, этапов, услуг и доп соглашений

**Шаг 9: План интеграции с существующим кодом**
- Обновить типы в `src/types/project-v3.ts` - добавить новые интерфейсы
- Обновить `supabaseDataStore.ts` - добавить методы для работы с файлами, этапами, услугами, доп соглашениями
- Обновить хук `useSupabaseData.ts` - добавить хуки для новых данных
- Обеспечить обратную совместимость - проекты без этапов/услуг должны работать

### 4. **Playwright** (после реализации)

Создай автоматические тесты:

**Тесты для файлов проекта:**
- Загрузка файла при создании проекта
- Загрузка нескольких файлов
- Просмотр списка файлов проекта
- Скачивание файла
- Удаление файла (проверить права доступа)
- Обработка ошибок загрузки (неверный тип файла, слишком большой размер)

**Тесты для этапов проекта:**
- Создание проекта с этапами (чекбокс включен)
- Добавление/удаление этапов
- Валидация дат этапов
- Создание проекта без этапов (чекбокс выключен)

**Тесты для дополнительных услуг:**
- Создание проекта с доп услугами (чекбокс включен)
- Выбор услуг из списка
- Добавление кастомной услуги
- Создание проекта без доп услуг

**Тесты для доп соглашений:**
- Добавление доп соглашения к действующему проекту (только для отдела закупок)
- Просмотр списка доп соглашений
- Загрузка файла доп соглашения
- Проверка прав доступа (только отдел закупок)

## [КОНТЕКСТ ПРОЕКТА - КРИТИЧЕСКИ ВАЖНО]

### Архитектура проекта:
- **Frontend:** React 18 + TypeScript (strict mode) + Vite
- **UI:** Shadcn/UI + Tailwind CSS
- **Backend:** Supabase (PostgreSQL + Storage + Auth)
- **State:** React Query (если используется) или хуки из useSupabaseData.ts
- **Стиль:** Функциональные компоненты с хуками
- **Импорты:** Абсолютные пути (@/components/...)

### Текущая структура проекта:
- **Форма создания проекта:** `src/pages/CreateProjectProcurement.tsx`
- **Типы проектов:** `src/types/project-v3.ts`
- **DataStore:** `src/lib/supabaseDataStore.ts`
- **Хуки:** `src/hooks/useSupabaseData.ts`
- **Страница проектов:** `src/pages/Projects.tsx`
- **Утверждение проектов:** `src/pages/ProjectApproval.tsx`

### КРИТИЧЕСКИ ВАЖНЫЕ ПРАВИЛА:

1. **НЕ ЛОМАТЬ СУЩЕСТВУЮЩИЙ ФУНКЦИОНАЛ:**
   - Все изменения должны быть обратно совместимы
   - Проекты без этапов/услуг должны работать как раньше
   - Существующие проекты должны отображаться корректно
   - НЕ удалять существующие поля и компоненты

2. **Использовать существующие паттерны:**
   - Следовать стилю кода в CreateProjectProcurement.tsx
   - Использовать те же UI компоненты (Card, Button, Input, Label, etc.)
   - Использовать те же хуки (useToast, useAuth, useSupabaseData)
   - Следовать структуре данных как в ProjectV3

3. **Работа с Supabase:**
   - ВСЕ данные хранить в Supabase (не только localStorage)
   - Использовать supabaseDataStore для всех операций с БД
   - Файлы хранить в Supabase Storage (bucket для проектов)
   - Использовать RLS (Row Level Security) для безопасности

4. **Обработка ошибок:**
   - Все операции с файлами должны иметь обработку ошибок
   - Показывать понятные сообщения пользователю через toast
   - Логировать ошибки в консоль для отладки
   - Graceful degradation - если что-то не работает, не ломать весь проект

5. **UX соображения:**
   - Все новые поля должны быть интуитивно понятны
   - Чекбоксы должны открывать/скрывать соответствующие секции плавно
   - Загрузка файлов должна показывать прогресс
   - Валидация должна быть понятной (какие файлы можно загружать, размер, etc.)

6. **Права доступа:**
   - Загрузка файлов: только отдел закупок при создании проекта, потом команда проекта
   - Добавление доп соглашений: только отдел закупок
   - Удаление файлов: только тот, кто загрузил, или админ
   - Просмотр файлов: команда проекта и выше

## [ПОШАГОВЫЙ ПЛАН РЕАЛИЗАЦИИ]

### Этап 1: Подготовка БД и типов
1. Создать/обновить миграцию Supabase:
   - Таблица `project_files`
   - Поля `stages` и `additional_services` в таблице `projects` (JSONB)
   - Таблица `project_amendments`
   - Bucket в Storage (если не существует)
2. Обновить типы в `src/types/project-v3.ts`

### Этап 2: Backend (supabaseDataStore)
1. Добавить методы для работы с файлами
2. Добавить методы для работы с этапами
3. Добавить методы для работы с услугами
4. Добавить методы для работы с доп соглашениями

### Этап 3: Компоненты
1. Создать `ProjectFileManager.tsx`
2. Создать `ProjectStagesEditor.tsx`
3. Создать `AdditionalServicesSelector.tsx`
4. Создать `ProjectAmendmentForm.tsx`

### Этап 4: Интеграция в форму создания
1. Модифицировать `CreateProjectProcurement.tsx`:
   - Добавить чекбокс "Этапы проекта" + ProjectStagesEditor
   - Добавить чекбокс "Дополнительные услуги" + AdditionalServicesSelector
   - Добавить ProjectFileManager для загрузки файлов
2. Обновить handleSubmit для сохранения новых данных

### Этап 5: Отображение в проекте
1. Модифицировать страницу проекта для отображения:
   - Списка файлов
   - Этапов проекта
   - Дополнительных услуг
   - Доп соглашений
2. Добавить возможность добавления доп соглашений (для отдела закупок)

### Этап 6: Тестирование
1. Протестировать создание проекта со всеми новыми полями
2. Протестировать создание проекта без новых полей (обратная совместимость)
3. Протестировать загрузку/удаление файлов
4. Протестировать добавление доп соглашений
5. Протестировать права доступа

## [КОМАНДА ДЛЯ CURSOR AI]

Реализуй все изменения согласно плану, строго следуя инструкциям по MCP серверам и правилам контекста проекта. Все изменения должны быть обратно совместимы и не ломать существующий функционал.


