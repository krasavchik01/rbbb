# 🚀 СИСТЕМА УЛУЧШЕНИЯ ПРОМПТОВ ДЛЯ CURSOR AI

## 📋 Как это работает

Ты пишешь свой запрос, а я его переписываю с автоматическим добавлением инструкций для использования MCP серверов:
- **Sequential Thinking** - для глубокого анализа и пошагового решения
- **Context7** - для получения актуальной документации библиотек
- **Playwright** - для автоматического тестирования кода

---

## 🎯 ШАБЛОН УЛУЧШЕННОГО ПРОМПТА

### Структура:
1. **Твой оригинальный запрос**
2. **Инструкции по использованию MCP серверов**
3. **Контекст проекта** (если нужен)

---

## 📝 ПРИМЕРЫ

### Пример 1: Простой запрос
**Твой запрос:** "Добавь валидацию формы"

**Улучшенный промпт:**
```
[ТВОЙ ЗАПРОС]
Добавь валидацию формы регистрации пользователя с проверкой email, пароля и подтверждения пароля.

[ИНСТРУКЦИИ ПО MCP СЕРВЕРАМ]
ОБЯЗАТЕЛЬНО используй следующие MCP серверы в указанной последовательности:

1. **Sequential Thinking** (mcp_sequential-thinking_sequentialthinking):
   - Используй для анализа требований валидации
   - Разбей задачу на шаги: какие поля валидировать, какие правила применять, как показывать ошибки
   - Продумай edge cases и граничные случаи

2. **Context7** (mcp_context7_resolve-library-id, mcp_context7_get-library-docs):
   - Если используешь библиотеки валидации (например, zod, react-hook-form, yup):
     - Сначала вызови mcp_context7_resolve-library-id с именем библиотеки
     - Затем mcp_context7_get-library-docs для получения актуальной документации
   - Используй актуальные best practices из документации

3. **Playwright** (после реализации):
   - Создай тесты для проверки валидации
   - Протестируй все сценарии: пустые поля, неверный формат email, короткий пароль, несовпадение паролей

[КОНТЕКСТ ПРОЕКТА]
Проект: React + TypeScript + Vite + Supabase
UI: Shadcn/UI + Tailwind
Стиль: Функциональные компоненты с хуками
Импорты: Абсолютные пути (@/components/...)
```

### Пример 2: Работа с библиотекой
**Твой запрос:** "Интегрируй react-query для кеширования"

**Улучшенный промпт:**
```
[ТВОЙ ЗАПРОС]
Интегрируй React Query (TanStack Query) для кеширования данных из Supabase вместо текущего подхода.

[ИНСТРУКЦИИ ПО MCP СЕРВЕРАМ]
ОБЯЗАТЕЛЬНО используй следующие MCP серверы в указанной последовательности:

1. **Context7** (ПЕРВЫМ ДЕЛОМ):
   - Вызови mcp_context7_resolve-library-id с запросом "react-query" или "tanstack-query"
   - Получи актуальную документацию через mcp_context7_get-library-docs
   - Изучи best practices для интеграции с Supabase
   - Используй актуальные паттерны из документации

2. **Sequential Thinking** (mcp_sequential-thinking_sequentialthinking):
   - Проанализируй текущую архитектуру (useSupabaseData.ts, supabaseDataStore.ts)
   - Разбей задачу на шаги:
     a) Установка зависимостей
     b) Настройка QueryClient
     c) Создание хуков для запросов
     d) Миграция существующих компонентов
     e) Обработка ошибок и loading состояний
   - Продумай стратегию кеширования и инвалидации

3. **Playwright** (после реализации):
   - Протестируй загрузку данных
   - Проверь кеширование (повторные запросы не должны идти к серверу)
   - Протестируй инвалидацию кеша при мутациях

[КОНТЕКСТ ПРОЕКТА]
Текущая архитектура:
- src/hooks/useSupabaseData.ts - существующие хуки
- src/lib/supabaseDataStore.ts - основной класс для работы с БД
Проект использует React 18, TypeScript strict mode
```

### Пример 3: Создание компонента
**Твой запрос:** "Создай компонент календаря для выбора дат"

**Улучшенный промпт:**
```
[ТВОЙ ЗАПРОС]
Создай компонент календаря для выбора дат в формате Shadcn/UI, совместимый с существующим дизайном проекта.

[ИНСТРУКЦИИ ПО MCP СЕРВЕРАМ]
ОБЯЗАТЕЛЬНО используй следующие MCP серверы в указанной последовательности:

1. **Context7** (ПЕРВЫМ ДЕЛОМ):
   - Вызови mcp_context7_resolve-library-id для "react-day-picker" или "date-fns"
   - Получи документацию через mcp_context7_get-library-docs
   - Проверь документацию Shadcn/UI Calendar компонента
   - Используй актуальные API и паттерны

2. **Sequential Thinking** (mcp_sequential-thinking_sequentialthinking):
   - Проанализируй требования: диапазон дат, один день, несколько дней?
   - Продумай UX: как показывать выбранные даты, как отображать недоступные даты
   - Разбей на компоненты: Calendar, DatePicker, DateRangePicker (если нужно)
   - Продумай интеграцию с формами (react-hook-form если используется)

3. **Playwright** (после реализации):
   - Протестируй открытие/закрытие календаря
   - Протестируй выбор даты
   - Протестируй валидацию (например, нельзя выбрать прошлые даты)
   - Протестируй на мобильных устройствах

[КОНТЕКСТ ПРОЕКТА]
Проект использует Shadcn/UI компоненты
Стиль: Tailwind CSS, градиенты
Адаптивность: Desktop + Mobile
```

---

## 🔧 СТАНДАРТНЫЕ БЛОКИ ДЛЯ КОПИРОВАНИЯ

### Блок для работы с библиотеками:
```
[ИНСТРУКЦИИ ПО MCP СЕРВЕРАМ]
ОБЯЗАТЕЛЬНО используй следующие MCP серверы в указанной последовательности:

1. **Context7** (ПЕРВЫМ ДЕЛОМ):
   - Вызови mcp_context7_resolve-library-id с именем библиотеки: "[НАЗВАНИЕ_БИБЛИОТЕКИ]"
   - Получи актуальную документацию через mcp_context7_get-library-docs
   - Изучи best practices и актуальные API
   - Используй паттерны из официальной документации

2. **Sequential Thinking** (mcp_sequential-thinking_sequentialthinking):
   - Проанализируй задачу пошагово
   - Разбей на логические этапы
   - Продумай edge cases и граничные случаи
   - Составь план реализации

3. **Playwright** (после реализации):
   - Создай автоматические тесты для нового функционала
   - Протестируй все сценарии использования
   - Проверь на разных устройствах (desktop/mobile)
```

### Блок для рефакторинга:
```
[ИНСТРУКЦИИ ПО MCP СЕРВЕРАМ]
ОБЯЗАТЕЛЬНО используй следующие MCP серверы в указанной последовательности:

1. **Sequential Thinking** (mcp_sequential-thinking_sequentialthinking):
   - Проанализируй текущий код глубоко
   - Определи проблемы и узкие места
   - Продумай оптимальную архитектуру
   - Составь план рефакторинга пошагово

2. **Context7** (если используются библиотеки):
   - Проверь актуальные best practices для используемых библиотек
   - Получи документацию если нужно

3. **Playwright** (после рефакторинга):
   - Убедись что все тесты проходят
   - Добавь тесты если нужно
   - Проверь что функционал не сломался
```

### Блок для создания нового функционала:
```
[ИНСТРУКЦИИ ПО MCP СЕРВЕРАМ]
ОБЯЗАТЕЛЬНО используй следующие MCP серверы в указанной последовательности:

1. **Sequential Thinking** (mcp_sequential-thinking_sequentialthinking):
   - Проанализируй требования глубоко
   - Разбей задачу на подзадачи
   - Продумай архитектуру решения
   - Определи необходимые компоненты и хуки
   - Продумай edge cases и обработку ошибок

2. **Context7** (если используются новые библиотеки):
   - Вызови mcp_context7_resolve-library-id для необходимых библиотек
   - Получи документацию через mcp_context7_get-library-docs
   - Используй актуальные best practices

3. **Playwright** (после реализации):
   - Создай комплексные тесты для нового функционала
   - Протестируй все сценарии использования
   - Протестируй на разных устройствах
```

---

## ⚡ БЫСТРЫЙ ФОРМАТ

Если нужно быстро - просто напиши свой запрос, и я добавлю стандартный блок с MCP серверами.

---

## 🎯 ПРИНЦИПЫ

1. **Sequential Thinking** - всегда для анализа и планирования
2. **Context7** - всегда когда нужна документация библиотек (ПЕРВЫМ ДЕЛОМ)
3. **Playwright** - всегда после реализации для тестирования

**Порядок использования:**
- Для новых библиотек: Context7 → Sequential Thinking → Реализация → Playwright
- Для рефакторинга: Sequential Thinking → Context7 (если нужно) → Реализация → Playwright
- Для простых задач: Sequential Thinking → Реализация → Playwright


