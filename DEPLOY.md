# 🚀 Инструкция по деплою на Vercel

## Быстрый деплой

### Вариант 1: Через Vercel CLI (рекомендуется)

```bash
# 1. Установить Vercel CLI (если еще не установлен)
npm install -g vercel

# 2. Войти в аккаунт Vercel
vercel login

# 3. Задеплоить проект
vercel --prod
```

### Вариант 2: Через веб-интерфейс Vercel

1. Зайти на https://vercel.com
2. Нажать "Add New Project"
3. Подключить GitHub/GitLab/Bitbucket репозиторий
4. Vercel автоматически определит настройки:
   - **Framework Preset:** Vite
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm install`
5. Нажать "Deploy"

### Вариант 3: Если есть git репозиторий

```bash
# Просто запушить в main ветку (если настроен auto-deploy)
git add .
git commit -m "Deploy"
git push origin main
```

---

## Важно:

- **Проект уже собран** (`dist/` папка существует)
- **Настройки Vercel** уже в `vercel.json`
- После деплоя получишь ссылку типа: `https://your-project.vercel.app`

---

## Если есть проблемы:

1. Проверь что Vercel CLI установлен: `vercel --version`
2. Проверь сборку локально: `npm run build`
3. Убедись что есть интернет подключение





