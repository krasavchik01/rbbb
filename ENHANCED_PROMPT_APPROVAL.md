# 🚀 УЛУЧШЕННЫЙ ПРОМПТ: ИСПРАВЛЕНИЕ УТВЕРЖДЕНИЯ ПРОЕКТОВ

## [ТВОЙ ЗАПРОС]

Исправить проблемы в системе утверждения проектов и добавить новый функционал:

1. **Исправить выпадающий список партнеров:**
   - Зам. директор не видит выпадающий список партнеров при утверждении проекта
   - Проблема: список пустой или не отображается, хотя партнер обязателен для назначения

2. **Добавить чекбокс "Показывать сумму":**
   - При утверждении проекта добавить чекбокс "Показывать финансовую информацию"
   - Если чекбокс включен, добавить возможность выбрать кому из команды показывать сумму (чекбоксы для каждого члена команды)
   - Сохранять настройки видимости суммы в проекте

3. **Улучшить систему уведомлений:**
   - При утверждении проекта система должна оповещать ВСЕХ участников проекта сразу
   - Уведомления должны отправляться всем назначенным членам команды одновременно
   - Уведомление должно содержать информацию о проекте и их роли

## [ИНСТРУКЦИИ ПО MCP СЕРВЕРАМ]

**ОБЯЗАТЕЛЬНО используй следующие MCP серверы в указанной последовательности:**

### 1. **Sequential Thinking** (mcp_sequential-thinking_sequentialthinking) - ПЕРВЫМ ДЕЛОМ

Используй для глубокого анализа проблем:

**Шаг 1: Анализ проблемы с выпадающим списком партнеров**
- Изучи код в `src/pages/ProjectApproval.tsx` строки 73-81, 487-520
- Проблема: `availableEmployees` фильтруется через `mapEmployeeRoleToProjectRole`, но если в базе нет сотрудников с ролью 'partner', список будет пустым
- Проверь: правильно ли маппятся роли из базы данных (employees.role) в роли проектов
- Проверь: загружаются ли вообще сотрудники из Supabase через `useEmployees()`
- Определи: может быть проблема в том, что роль в базе называется не 'partner', а по-другому?

**Шаг 2: Анализ текущей логики отображения**
- Изучи условие `{isRoleSelected &&` на строке 520
- Проблема: для партнера `isRoleSelected` всегда `true` (строка 48-50), но если `employeesForRole` пустой, список не показывается
- Нужно: показывать список даже если он пустой, но с сообщением "Нет доступных партнеров" или исправить фильтрацию

**Шаг 3: Планирование исправления**
- Вариант 1: Исправить маппинг ролей - проверить какие роли реально есть в базе
- Вариант 2: Показывать всех сотрудников с ролью 'partner' независимо от маппинга
- Вариант 3: Добавить логирование для отладки - что приходит из базы
- Вариант 4: Показывать сообщение если список пустой, но чекбокс включен

**Шаг 4: Планирование функционала "Показывать сумму"**
- Структура данных: добавить в ProjectV3 поле `financialVisibility`:
  ```typescript
  financialVisibility: {
    enabled: boolean; // показывать ли сумму вообще
    visibleTo: string[]; // userId тех, кому показывать
  }
  ```
- UI: добавить чекбокс "Показывать финансовую информацию" в модальное окно утверждения
- UI: если чекбокс включен, показать список всех назначенных членов команды с чекбоксами
- Сохранение: сохранять настройки при утверждении проекта

**Шаг 5: Планирование улучшения уведомлений**
- Текущая логика: уведомления отправляются в `handleApprove` (строки 254-289), но только для партнера и PM отдельно
- Нужно: отправлять уведомление ВСЕМ членам команды одновременно
- Создать функцию `notifyAllTeamMembers` которая отправляет уведомление каждому участнику
- Уведомление должно содержать: название проекта, их роль, кто утвердил проект

**Шаг 6: Edge cases**
- Что если нет ни одного партнера в базе? Показывать ошибку или предупреждение?
- Что если чекбокс "Показывать сумму" включен, но никто не выбран? Валидация
- Что если уведомление не отправилось одному из участников? Обработка ошибок
- Обратная совместимость: старые проекты без `financialVisibility` должны работать

### 2. **Context7** (mcp_context7_resolve-library-id, mcp_context7_get-library-docs)

Используй для получения актуальной документации:

**Для работы с уведомлениями:**
- Если используется библиотека для уведомлений, получи документацию
- Проверь best practices для массовой отправки уведомлений

**Для работы с Supabase:**
- Получи документацию по запросам к таблице employees
- Проверь как правильно фильтровать сотрудников по ролям

### 3. **Sequential Thinking** (продолжение) - детальное планирование

**Шаг 7: Детальный план исправления выпадающего списка**

**Проблема 1: Пустой список партнеров**
```typescript
// Текущий код (строка 487):
const employeesForRole = availableEmployees.filter(emp => emp.role === projectRole.role);

// Проблема: если в базе нет сотрудников с ролью 'partner', список пустой
// Решение 1: Проверить что приходит из базы
console.log('Available employees:', availableEmployees);
console.log('Employees for partner role:', availableEmployees.filter(emp => emp.role === 'partner'));

// Решение 2: Исправить маппинг - возможно роль в базе называется по-другому
// Проверить: может быть 'deputy_director' или другая роль должна маппиться в 'partner'?

// Решение 3: Показывать всех сотрудников с ролью содержащей 'partner' или 'директор'
```

**Проблема 2: Условие отображения**
```typescript
// Текущий код (строка 520):
{isRoleSelected && (
  <div className="ml-7 space-y-2">
    <Select>...</Select>
  </div>
)}

// Проблема: если employeesForRole пустой, Select показывается но без опций
// Решение: показывать Select всегда для партнера (т.к. обязателен), но с сообщением если пусто
{isRoleSelected && (
  <div className="ml-7 space-y-2">
    {employeesForRole.length > 0 ? (
      <Select>...</Select>
    ) : (
      <div className="text-sm text-yellow-600 dark:text-yellow-400 p-2 bg-yellow-50 dark:bg-yellow-900/20 rounded">
        ⚠️ Нет доступных партнеров в базе. Проверьте настройки сотрудников.
      </div>
    )}
  </div>
)}
```

**Шаг 8: Детальный план функционала "Показывать сумму"**

**Структура данных:**
```typescript
// В ProjectV3 добавить:
interface ProjectV3 {
  // ... существующие поля
  financialVisibility?: {
    enabled: boolean;
    visibleTo: string[]; // userId членов команды
  };
}
```

**UI компонент:**
```typescript
// В модальном окне утверждения добавить секцию:
<div className="border rounded-lg p-4 space-y-3">
  <div className="flex items-center gap-3">
    <input
      type="checkbox"
      checked={showFinancialInfo}
      onChange={(e) => setShowFinancialInfo(e.target.checked)}
    />
    <Label>Показывать финансовую информацию команде</Label>
  </div>
  
  {showFinancialInfo && (
    <div className="ml-7 space-y-2">
      <Label className="text-sm text-muted-foreground">
        Выберите кому показывать сумму:
      </Label>
      {updatedProject.team.map(member => (
        <div key={member.userId} className="flex items-center gap-2">
          <input
            type="checkbox"
            checked={financialVisibleTo.includes(member.userId)}
            onChange={(e) => {
              if (e.target.checked) {
                setFinancialVisibleTo([...financialVisibleTo, member.userId]);
              } else {
                setFinancialVisibleTo(financialVisibleTo.filter(id => id !== member.userId));
              }
            }}
          />
          <Label className="text-sm">
            {member.userName} ({ROLE_LABELS[member.role]})
          </Label>
        </div>
      ))}
    </div>
  )}
</div>
```

**Шаг 9: Детальный план улучшения уведомлений**

**Текущая логика (строки 254-289):**
- Отправляет уведомление только партнеру через `notifyProjectApproved`
- Отправляет уведомление только PM через `notifyPMAssigned`
- Остальным через `notifyTeamMemberAdded`

**Новая логика:**
```typescript
// Создать функцию для уведомления всех участников
const notifyAllTeamMembers = async (project: ProjectV3, approverName: string) => {
  const notifications = project.team.map(member => {
    const employee = availableEmployees.find(e => e.id === member.userId);
    if (!employee) return null;

    // Определяем тип уведомления по роли
    if (member.role === 'partner') {
      return notifyProjectApproved({
        projectName: project.name,
        partnerId: member.userId,
        partnerName: employee.name,
        approverName: approverName
      });
    } else if (member.role === 'project_manager') {
      return notifyPMAssigned({
        projectName: project.name,
        pmId: member.userId,
        pmName: employee.name,
        partnerName: approverName,
        projectId: project.id
      });
    } else {
      return notifyTeamMemberAdded({
        projectName: project.name,
        memberId: member.userId,
        memberName: employee.name,
        role: ROLE_LABELS[member.role as UserRole] || member.role,
        assignerName: approverName,
        projectId: project.id
      });
    }
  });

  // Отправляем все уведомления параллельно
  await Promise.all(notifications.filter(Boolean));
};
```

### 4. **Playwright** (после реализации)

Создай автоматические тесты:

**Тесты для выпадающего списка партнеров:**
- Проверить что список партнеров отображается при открытии модального окна
- Проверить что можно выбрать партнера из списка
- Проверить что если партнеров нет, показывается предупреждение
- Проверить что нельзя утвердить проект без партнера

**Тесты для функционала "Показывать сумму":**
- Проверить что чекбокс "Показывать финансовую информацию" отображается
- Проверить что при включении чекбокса показываются чекбоксы для каждого члена команды
- Проверить что можно выбрать несколько участников
- Проверить что настройки сохраняются при утверждении проекта
- Проверить что в проекте отображается сумма только выбранным участникам

**Тесты для уведомлений:**
- Проверить что при утверждении проекта отправляются уведомления всем участникам
- Проверить что каждое уведомление содержит правильную информацию
- Проверить что уведомления отправляются даже если один из участников не найден

## [КОНТЕКСТ ПРОЕКТА - КРИТИЧЕСКИ ВАЖНО]

### Текущая структура:
- **Файл утверждения:** `src/pages/ProjectApproval.tsx`
- **Типы проектов:** `src/types/project-v3.ts`
- **Роли:** `src/types/roles.ts`
- **Уведомления:** `src/lib/projectNotifications.ts`
- **DataStore:** `src/lib/supabaseDataStore.ts`
- **Хуки:** `src/hooks/useSupabaseData.ts` (useEmployees)

### КРИТИЧЕСКИ ВАЖНЫЕ ПРАВИЛА:

1. **НЕ ЛОМАТЬ СУЩЕСТВУЮЩИЙ ФУНКЦИОНАЛ:**
   - Все изменения должны быть обратно совместимы
   - Старые проекты без `financialVisibility` должны работать (по умолчанию сумма не показывается)
   - Существующая логика уведомлений должна продолжать работать

2. **Исправить проблему с партнерами:**
   - Добавить логирование для отладки (console.log) чтобы понять что приходит из базы
   - Показывать предупреждение если партнеров нет, но не блокировать интерфейс
   - Проверить маппинг ролей - возможно нужно добавить дополнительные роли

3. **Функционал "Показывать сумму":**
   - По умолчанию сумма НЕ показывается (для безопасности)
   - Зам. директор может выбрать кому показывать
   - Настройки сохраняются в проекте и используются при отображении финансов

4. **Улучшение уведомлений:**
   - Уведомления должны отправляться ВСЕМ участникам одновременно
   - Каждое уведомление должно быть персонализировано (содержать роль участника)
   - Обработка ошибок: если одно уведомление не отправилось, остальные должны отправиться

5. **UX соображения:**
   - Чекбокс "Показывать сумму" должен быть понятным
   - Список участников для выбора должен быть удобным (можно выбрать всех одной кнопкой)
   - Предупреждения должны быть информативными

## [ПОШАГОВЫЙ ПЛАН РЕАЛИЗАЦИИ]

### Этап 1: Исправление выпадающего списка партнеров
1. Добавить логирование в `ProjectApproval.tsx`:
   - Логировать `realEmployees` из `useEmployees()`
   - Логировать `availableEmployees` после маппинга
   - Логировать `employeesForRole` для роли 'partner'

2. Исправить отображение:
   - Показывать Select всегда для партнера (т.к. обязателен)
   - Если список пустой, показывать предупреждение вместо Select
   - Добавить проверку: если партнеров нет, показать ошибку при попытке утвердить

3. Проверить маппинг ролей:
   - Убедиться что роль 'partner' правильно маппится из базы
   - Возможно добавить дополнительные варианты маппинга

### Этап 2: Добавление функционала "Показывать сумму"
1. Обновить типы:
   - Добавить `financialVisibility` в `ProjectV3` (опциональное поле)

2. Добавить состояние в компонент:
   - `showFinancialInfo` - boolean
   - `financialVisibleTo` - string[] (userId)

3. Добавить UI в модальное окно:
   - Чекбокс "Показывать финансовую информацию"
   - Список участников с чекбоксами (если чекбокс включен)

4. Сохранять настройки:
   - При утверждении проекта сохранять `financialVisibility` в проект

### Этап 3: Улучшение уведомлений
1. Создать функцию `notifyAllTeamMembers`:
   - Принимает проект и имя утверждающего
   - Отправляет уведомления всем участникам параллельно
   - Обрабатывает ошибки

2. Обновить `handleApprove`:
   - Заменить текущую логику уведомлений на вызов `notifyAllTeamMembers`

3. Улучшить сообщения уведомлений:
   - Добавить информацию о роли участника
   - Добавить информацию о проекте

### Этап 4: Тестирование
1. Протестировать выпадающий список партнеров
2. Протестировать функционал "Показывать сумму"
3. Протестировать уведомления
4. Протестировать обратную совместимость

## [КОМАНДА ДЛЯ CURSOR AI]

Реализуй все исправления и улучшения согласно плану, строго следуя инструкциям по MCP серверам и правилам контекста проекта. Все изменения должны быть обратно совместимы и не ломать существующий функционал. Особое внимание удели исправлению проблемы с выпадающим списком партнеров - добавь логирование для отладки и исправь отображение.





