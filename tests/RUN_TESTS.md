# 🧪 Инструкция по запуску Playwright тестов

## Быстрый старт

### 1. Установка (уже выполнено)
```bash
npm install -D @playwright/test
npx playwright install chromium
```

### 2. Запуск dev сервера
Откройте **отдельный терминал** и запустите:
```bash
npm run dev
```
Оставьте его работать в фоне.

### 3. Запуск тестов

**Все тесты:**
```bash
npm run test
```

**С UI (рекомендуется для первого раза):**
```bash
npm run test:ui
```

**В видимом браузере (headed mode):**
```bash
npm run test:headed
```

**Отладка конкретного теста:**
```bash
npm run test:debug
```

**Конкретный тест файл:**
```bash
npx playwright test tests/project-files.spec.ts
```

## 📋 Список тестов

1. **tests/project-files.spec.ts** - Тесты файлов проекта
   - Загрузка файла при создании проекта
   - Отображение файлов в деталях проекта
   - Скачивание файла
   - Удаление файла
   - Валидация типа и размера файла

2. **tests/project-stages.spec.ts** - Тесты этапов проекта
   - Добавление этапов при создании проекта
   - Отображение этапов в деталях проекта
   - Добавление нескольких этапов
   - Удаление этапа

3. **tests/project-services.spec.ts** - Тесты дополнительных услуг
   - Добавление услуг при создании проекта
   - Отображение услуг в деталях проекта
   - Выбор услуги из списка

4. **tests/project-amendments.spec.ts** - Тесты доп соглашений
   - Добавление доп соглашения (только для отдела закупок)
   - Отображение доп соглашений
   - Валидация формы
   - Скачивание файла доп соглашения

5. **tests/project-creation-flow.spec.ts** - Интеграционный тест
   - Полный цикл создания проекта со всеми новыми полями
   - Проверка сохранения и отображения всех данных

## ⚙️ Настройка перед запуском

### Важно!
Перед запуском тестов нужно:

1. **Создать тестовых пользователей в Supabase:**
   - procurement@test.com (отдел закупок)
   - ceo@test.com (CEO)
   - deputy@test.com (зам директора)

2. **Обновить данные входа в `tests/helpers/auth.ts`:**
   - Замените email и пароли на реальные тестовые данные

3. **Проверить `playwright.config.ts`:**
   - `baseURL` должен указывать на ваш dev сервер (по умолчанию `http://localhost:5173`)
   - Если используете другой порт - измените в конфиге

## 🔍 Что тестируется

✅ Загрузка файлов в Supabase Storage  
✅ Сохранение этапов проекта в JSONB  
✅ Сохранение дополнительных услуг в JSONB  
✅ Создание доп соглашений  
✅ Отображение всех данных в деталях проекта  
✅ Права доступа (кнопка доп соглашений только для закупок)  
✅ Валидация форм  

## 📊 Просмотр результатов

После запуска тестов:
- HTML отчет: `playwright-report/index.html`
- Скриншоты ошибок: `test-results/`
- Видео (если включено): `test-results/`

Откройте HTML отчет:
```bash
npx playwright show-report
```

## 🐛 Отладка

Если тест падает:

1. Запустите в debug режиме:
   ```bash
   npm run test:debug
   ```

2. Или в headed режиме (видимый браузер):
   ```bash
   npm run test:headed
   ```

3. Проверьте консоль браузера на ошибки

4. Убедитесь, что dev сервер запущен на правильном порту

## ⚠️ Примечания

- Тесты требуют запущенного dev сервера
- Некоторые тесты могут требовать предварительной настройки данных в БД
- Тесты используют `data-testid` атрибуты для надежного поиска элементов
- Если тесты падают из-за таймаутов - увеличьте `timeout` в `playwright.config.ts`

